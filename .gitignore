__pycache__/
*.pyc
build/
dist/*
!dist/findany.exe
out/
logs/
findany.toml

# 交付材料含 SecretKey 与明文 ProductKey/Hash，严禁入库（delivery-sop.md 第 6 条）
doc/
config.json
crash.log
startup.log
check_env_output.txt
*.spec
.venv/
venv/

# Rust（v2.0）
/target/
# 参考资料：v1 Python 实现（git archive 导出，非源码，不入库）
/legacy/

# just msi 按 mesa/ 实际 dll 列表生成的 WiX 片段（勿入库）
/wix/mesa.generated.wxi
# just msi 按运行时图标 PNG 列表生成的 WiX 片段（勿入库,build script 自动重写）
/wix/icon.generated.wxi
# 旧图标/资源备份（替换品牌时挪出 → 备份目录,不入库）
_bak_*/
_bak_*/**

# 运行产出的批次目录（out/YYYY-MM-DD_HH-MM 之类跑飞的产物，不入库）
/20??-??-??_??-??/
/20??-??-??_??-??_*/

# Mesa 软件 OpenGL 交付材料（59MB 第三方 DLL，随包分发不入库）
/mesa/

# 10W+ 压测数据（本地生成，约 2.9GB，不入库）
# 生成/自检脚本与说明保留在 tests/benchdata/10w_mix/，数据本体按需本地生成
/tests/benchdata/*/real/
/tests/benchdata/*/fake/
/tests/benchdata/*/manifest.json

# cargo-wix �ڲֿ���������ʱ wxs(ÿ��������),�����
/ETest-*.wxs
/findany-*.wxs

# runtime artifacts (progress node / dir index / bad-file list / done log)
/_index_*.jsonl
/_index_*.meta.json
/_run_*.jsonl
/_resume*.json
/_resume_retry_done.txt
/_bad_files.txt
